# Remap bashrc to bash_profile so that one setup for multi system.
# !important: do not add any line here.
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# Added by Windsurf
export PATH="/Users/diz/.codeium/windsurf/bin:$PATH"
